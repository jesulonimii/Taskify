import React from 'react';
import {HomeIcon, PlusIcon} from "@heroicons/react/20/solid/index.js";

function Footer(props) {
    return (
        <div className={`${props.className} text-white  items-center justify-center bg-red-300 h-[10%] w-full flex`}>

        </div>
    );
}

export default Footer;